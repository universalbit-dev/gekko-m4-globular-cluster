###############################################################
#   Exchange Simulator Mode & Core Asset/Currency Settings    #
###############################################################
# --- exchange identifier for simulator mode
EXCHANGESIMULATOR=exchangesimulator
# --- id used by simulator data source
SIMULATOR_DATA_ID=exchangesimulator
# --- core currency for accounting and reporting
CURRENCY=GaiaNut
# --- core asset for trading
ASSET=GaiaBolt

###############################################################
#         CCXT Market Data Plugin Settings                    #
###############################################################
# --- enable CCXT-based market data plugin (true/false)
CCXT_MARKET_DATA_ENABLED=true
# --- exchange to use for CCXT market data
CCXT_MARKET_DATA_EXCHANGE=kraken
# --- trading pair to fetch from CCXT
CCXT_MARKET_DATA_PAIR=BTC/EUR
# --- candle size used by CCXT plugin (human readable)
CCXT_MARKET_DATA_CANDLE_SIZE=1h
# --- fetch interval (ms) for CCXT market data
CCXT_MARKET_DATA_FETCH_INTERVAL=3600000
# --- file path to output fetched ohlcv as CSV
CCXT_MARKET_DATA_OUTPUT_CSV=./logs/csv/ohlcv_ccxt_data.csv
# --- file path to output fetched ohlcv as JSON
CCXT_MARKET_DATA_OUTPUT_JSON=./logs/json/ohlcv/ohlcv_ccxt_data.json

###############################################################
#   Chart Recognition Script & Output Settings                #
###############################################################
# --- polling interval used by simulator components (ms)
INTERVAL_SIMULATOR_MS=3600000
# --- directory where trained models are stored
MODEL_DIR=./trained_ohlcv
# --- CSV output path for ohlcv predictions
OUT_CSV_PATH=./ohlcv_data_prediction.csv
# --- log path where generated signals are written
SIGNAL_LOG_PATH=./exchangesimulator_signal.log

###############################################################
#         CCXT Market Data & Trading Settings                 #
###############################################################
# --- exchange used for trading (CCXT)
# NOTE: API_KEY is intentionally omitted for testing purposes.
# Do not include sensitive API credentials in this file when testing.
EXCHANGE=kraken
# --- API key for exchange (left blank for safety)
KEY=
# --- API secret for exchange (left blank for safety)
SECRET=
# --- order pair for trading
PAIR=BTC/EUR
# --- default order amount (units of asset)
ORDER_AMOUNT=0.0001
# --- minimum allowed order amount
MIN_ORDER_AMOUNT=0.00008
# --- maximum allowed order amount
MAX_ORDER_AMOUNT=0.0001

###############################################################
#   CCXT Explorer & OHLCV Settings                            #
###############################################################
# --- comma-separated candle sizes to fetch for OHLCV explorer
OHLCV_CANDLE_SIZE=1m,5m,15m,1h
# --- main processing interval (ms)
INTERVAL_MS=3600000
# --- interval used for high-frequency tasks (ms)
INTERVAL_HIGH=60000
# --- default processing interval (ms)
INTERVAL_DEFAULT=300000
# --- lower-frequency interval (ms)
INTERVAL_LOW=780000
# --- number of candles to fetch per request
FETCH_LIMIT=60

###############################################################
#                  TRAINERS                                   #
###############################################################
# --- training loop polling interval (ms)
TRAIN_INTERVAL_MS=60000
# --- multi-interval processing loop interval (ms)
MULTI_INTERVAL_MS=60000
# --- comma-separated timeframes used for training OHLCV models
TRAIN_OHLCV_TIMEFRAMES=1m,5m,15m,1h
# --- directory where trainer expects OHLCV JSON files
TRAIN_OHLCV_JSON_DIR=../logs/json/ohlcv

# --- micro trading sell threshold (fraction, negative means loss)
MICRO_SELL_THRESHOLD=-0.05 
# --- hysteresis applied to micro decisions to avoid flip-flopping
MICRO_HYSTERESIS=0.02 
# --- minimum probability required to consider micro trade (0-1)
MICRO_MIN_PROB=0.65 
# --- minimum score threshold for micro decisions
MICRO_MIN_SCORE=0.01 
# --- probability strategy used for micro decisions (e.g., sigmoid)
MICRO_PROB_STRATEGY=sigmoid 
# --- scale factor applied to probability outputs
MICRO_PROB_SCALE=1.0 
# --- maximum score cap for micro scoring
MICRO_MAX_SCORE=100 
# --- (optional) path to probability calibration file (commented out)
#MICRO_PROB_CALIB_PATH=./prob_calib.json 
# --- verbose logging for micro trainer (0 = off)
MICRO_VERBOSE=0

# --- adaptive PV/VM thresholds used for dynamic sizing/timing
PVVM_THRESHOLDS=8,13,21,34,55
# --- adaptive PVD thresholds used for dynamic sizing/timing
PVD_THRESHOLDS=8,13,21,34,55
# --- dynamic window sizes used in strategy selection
DYNAMIC_WINDOWS=5,8,13,21
# --- multiplicative factors applied to dynamic window outputs
DYNAMIC_FACTORS=0.8,1.0,1.13,1.21

###############################################################
#                  CHALLENGE/Analysis Settings                #
###############################################################
# --- challenge loop polling interval (ms)
CHALLENGE_INTERVAL_MS=60000
# --- window size used for challenge/backtesting analysis (candles)
WINDOW_SIZE=50
# --- minimum win rate required to pass challenge filters
CHALLENGE_MIN_WIN_RATE=0.618
# --- timeframes used by challenge analysis
CHALLENGE_TIMEFRAMES=1m,5m,15m,1h
# --- ATR period used in challenge analysis
CHALLENGE_ATR_PERIOD=14
# --- single-model selection for challenge ('tf' or other)
CHALLENGE_SINGLE_MODEL=tf
# --- challenge/logging loop polling interval (ms)
CHLOG_INTERVAL_MS=60000
# --- how far into the future predictions are offset (candles)
FUTURE_OFFSET=2
# --- list of models used for challenge logging (comma-separated)
CHLOG_MODEL_LIST=convnet,tf
# --- timeframes used for challenge logging
CHLOG_TIMEFRAMES=1m,5m,15m,1h
# --- maximum number of rows to keep in challenge log
CHLOG_MAX_ROWS=10000
# --- label to treat as neutral/uncertain in challenge logs
CHLOG_NEUTRAL_RESULT=loss

###############################################################
#        Microstructure / Macrostructure TimeFrames           #
###############################################################
# --- primary microstructure timeframe(s)
MICRO_TIMEFRAMES=15m
# --- macrostructure timeframes used across components
MACRO_TIMEFRAMES=1m,5m,15m,1h

###############################################################
#                  Microstructure Settings                    #
###############################################################
# NOTE: API_KEY is intentionally omitted for testing purposes.
# Do not include sensitive API credentials in this file when testing.
# --- microstructure exchange for live micro trades
MICRO_EXCHANGE=kraken
# --- API key for micro exchange (left blank for safety)
MICRO_KEY=
# --- API secret for micro exchange (left blank for safety)
MICRO_SECRET=
# --- trading pair used by microstructure layer
MICRO_PAIR=BTC/EUR
# --- log path for micro order activity
LOG_PATH=tools/logs/micro_order.log
# --- default micro order amount
MICRO_ORDER_AMOUNT=0.0001
# --- maximum micro order amount allowed
MICRO_MAX_ORDER_AMOUNT=0.0001

# --- OHLCV candle sizes used by micro layer
MICRO_OHLCV_CANDLE_SIZE=1m,5m
# --- index candle sizes used for indicators
INDEX_OHLCV_CANDLE_SIZE=1m,5m
# --- micro processing interval (ms)
MICRO_INTERVAL_MS=300000

# --- selected micro strategy name
MICRO_STRATEGY=Balanced+
# --- primary timeframes used by micro strategy
MICRO_PRIMARY_TF=1m,5m

###############################################################
#                  Macrostructure Settings                    #
###############################################################
# NOTE: API_KEY is intentionally omitted for testing purposes.
# Do not include sensitive API credentials in this file when testing.
# --- exchange used by macro trading layer
MACRO_EXCHANGE=kraken
# --- API key for macro layer (left blank for safety)
MACRO_KEY=
# --- API secret for macro layer (left blank for safety)
MACRO_SECRET=
# --- trading pair for macro layer
MACRO_PAIR=BTC/EUR
# --- default order amount for macro trades (may override above)
ORDER_AMOUNT=0.0001
# --- minimum allowed order amount enforced at macro level
MIN_ALLOWED_ORDER_AMOUNT=0.00008
# --- maximum allowed order amount enforced at macro level
MAX_ORDER_AMOUNT=0.0001
# --- interval after a trade is executed before next action (ms)
INTERVAL_AFTER_TRADE=30000
# --- interval after skipping a trade decision (ms)
INTERVAL_AFTER_SKIP=90000
# --- interval after holding a position (ms)
INTERVAL_AFTER_HOLD=180000
# --- interval to wait after an error before retrying (ms)
INTERVAL_AFTER_ERROR=60000
# --- symbol used for macro layer (same as pair)
SYMBOL=BTC/EUR
# --- directory for OHLCV JSON output from macro
OHLCV_JSON_DIR=tools/logs/json/ohlcv
# --- path for OHLCV JSON file used by macro layer
OHLCV_JSON_PATH=tools/logs/json/ohlcv/ohlcv_ccxt_data.json
# --- backtest results path (relative)
BACKTEST_JSON_PATH=../backtest/backtest_results.json
# --- macro backtest results path (local)
MACRO_BACKTEST_JSON_PATH=./backtest/backtest_results.json

# --- stop loss percentage (fraction of price)
STOP_LOSS_PCT=0.003
# --- take profit percentage (fraction of price)
TAKE_PROFIT_PCT=0.006
# --- minimum win rate required by macro decision logic
MACRO_MIN_WIN_RATE=0.2
# --- maximum acceptable volatility threshold for macro (domain-specific)
MACRO_MAX_VOLATILITY=100

# --- comma-separated list of models available to macro layer
MODEL_LIST=convnet,tf

###############################################################
#                  Evaluate AutoTune                          #
###############################################################
# --- metric key used by autotune scoring system
AUTOTUNE_SCORE_KEY=abs
# --- evaluate loop polling interval (ms)
EVALUATE_INTERVAL_MS=60000
# --- autotune execution interval (ms)
AUTOTUNE_INTERVAL_MS=60000
# --- autotune strategy selection
EVALUATE_STRATEGY=neuralnet_v2
# --- generate simple parameter permutations 
GENERATE_PERMUTATIONS=1

###############################################################
# Advanced/Debug/Extra                                        #
###############################################################
# --- enable debug logging (true/false)
DEBUG=true
# --- run in dry-run mode (no real orders)
DRY_RUN=true
# --- force dry-run even if live would be enabled
FORCE_DRY=true
# --- master flag to allow live execution (true/false)
ENABLE_LIVE=false
# --- retry delay when order submission fails (ms)
ORDER_SUBMISSION_RETRY_MS=15000
# --- throttle between order submissions (ms)
ORDER_THROTTLE_MS=300000
# --- macro scoring metric used for model evaluation
MACRO_METRIC=abs
# --- micro scoring metric used for model evaluation
MICRO_METRIC=abs
# --- verbosity level for backtest logs (0 = quiet)
BACKTEST_VERBOSE=0
# --- polling interval (ms) used during backtests
BACKTEST_INTERVAL_MS=10000

####################################################
# Order sizing & exchange constraints              #
####################################################
# --- fixed order size override (if used)
FIXED_ORDER_SIZE=0.0001               
# --- minimum allowed amount enforced globally
MIN_ALLOWED_ORDER_AMOUNT=0.00008       
# --- legacy/compat minimum order amount
MIN_ORDER_AMOUNT=0.00008              
# --- maximum allowed order amount enforced globally
MAX_ORDER_AMOUNT=0.0001               

#Persistence/Logs
# --- path to persist current position state
POSITION_STATE_FILE=./logs/position_state.json
# --- file to log CCXT order events
ORDER_LOG_FILE=./logs/ccxt_order.log

###############################################################
#                  Strategy Settings (Simulator)              #
###############################################################
# --- BBands period used for Bollinger Bands
BBANDS_TIMEPERIOD=20
# --- BBands number of standard deviations up
BBANDS_NBDEVUP=2.25
# --- BBands number of standard deviations down
BBANDS_NBDEVDN=2
# --- moving average period for BBands (SMA)
BBANDS_SMA=200
# --- moving average period for BBands (DEMA)
BBANDS_DEMA=200
# --- Gann angles used by BBands variant (comma-separated)
BBANDS_GANN_ANGLES=1,2,3

# --- RSIBULLBEARADX long SMA window
RSIBULLBEARADX_SMA_LONG=200
# --- RSIBULLBEARADX short SMA window
RSIBULLBEARADX_SMA_SHORT=50
# --- RSIBULLBEARADX bull RSI threshold
RSIBULLBEARADX_BULL_RSI=10
# --- RSIBULLBEARADX bull RSI high cutoff
RSIBULLBEARADX_BULL_RSI_HIGH=80
# --- RSIBULLBEARADX bull RSI low cutoff
RSIBULLBEARADX_BULL_RSI_LOW=60
# --- RSIBULLBEARADX bear RSI threshold
RSIBULLBEARADX_BEAR_RSI=15
# --- RSIBULLBEARADX bear RSI high cutoff
RSIBULLBEARADX_BEAR_RSI_HIGH=50
# --- RSIBULLBEARADX bear RSI low cutoff
RSIBULLBEARADX_BEAR_RSI_LOW=20
# --- RSIBULLBEARADX bull momentum high
RSIBULLBEARADX_BULL_MOD_HIGH=5
# --- RSIBULLBEARADX bull momentum low
RSIBULLBEARADX_BULL_MOD_LOW=-5
# --- RSIBULLBEARADX bear momentum high
RSIBULLBEARADX_BEAR_MOD_HIGH=15
# --- RSIBULLBEARADX bear momentum low
RSIBULLBEARADX_BEAR_MOD_LOW=-5
# --- RSIBULLBEARADX RSI smoothing period
RSIBULLBEARADX_RSI=13
# --- RSIBULLBEARADX ADX period
RSIBULLBEARADX_ADX=8
# --- RSIBULLBEARADX ADX high threshold
RSIBULLBEARADX_ADX_HIGH=70
# --- RSIBULLBEARADX ADX low threshold
RSIBULLBEARADX_ADX_LOW=50

# INVERTER
# --- DI period for inverter
INVERTER_DI=13
# --- DX period for inverter
INVERTER_DX=3

# SUPERTREND
# --- Supertrend ATR period
SUPERTREND_ATR=7
# --- Supertrend band factor
SUPERTREND_BAND_FACTOR=3

# NEURALNET REFINEMENTS
# --- long SMA period used by neuralnet features
NEURALNET_SMA_LONG=987
# --- short SMA period used by neuralnet features
NEURALNET_SMA_SHORT=50
# --- neuralnet buy threshold (prediction score)
NEURALNET_THRESHOLD_BUY=0.2
# --- neuralnet sell threshold (prediction score)
NEURALNET_THRESHOLD_SELL=-0.2
# --- neuralnet learning rate
NEURALNET_LEARNING_RATE=0.01
# --- neuralnet limit order size (fraction)
NEURALNET_LIMIT_ORDER=0.01
# --- neuralnet momentum parameter
NEURALNET_MOMENTUM=0.1
# --- neuralnet decay (regularization)
NEURALNET_DECAY=0.01
# --- neuralnet hodl threshold (int)
NEURALNET_HODL_THRESHOLD=1
# --- buffer length for price history in neuralnet
NEURALNET_PRICE_BUFFER_LEN=1597
# --- minimum number of predictions neuralnet must produce
NEURALNET_MIN_PREDICTIONS=1597

# NEURALNET (duplicate block left for compatibility)
# --- duplicate compatibility block for older configs (same as above)
NEURALNET_SMA_LONG=987
NEURALNET_SMA_SHORT=50
NEURALNET_THRESHOLD_BUY=0.2
NEURALNET_THRESHOLD_SELL=-0.2
NEURALNET_LEARNING_RATE=0.01
NEURALNET_LIMIT_ORDER=0.01
NEURALNET_MOMENTUM=0.1
NEURALNET_DECAY=0.01
NEURALNET_HODL_THRESHOLD=1
NEURALNET_PRICE_BUFFER_LEN=1597
NEURALNET_MIN_PREDICTIONS=1597

# NN
# --- NN buy threshold
NN_THRESHOLD_BUY=0.2
# --- NN sell threshold
NN_THRESHOLD_SELL=-0.2
# --- NN optimization method
NN_METHOD=adadelta
# --- NN learning rate
NN_LEARNING_RATE=0.01
# --- NN momentum
NN_MOMENTUM=0.0
# --- L1 decay regularization for NN
NN_L1_DECAY=0.001
# --- L2 decay regularization for NN
NN_L2_DECAY=0.001
# --- price buffer length for NN
NN_PRICE_BUFFER_LEN=987
# --- minimum predictions for NN before acting
NN_MIN_PREDICTIONS=144
# --- HODL threshold for NN decisions
NN_HODL_THRESHOLD=1
# --- scaling factor applied to NN inputs/outputs
NN_SCALE=1
# --- batch size for NN training/prediction
NN_BATCH_SIZE=1
# --- RSI period used by NN features
NN_RSI=13
# --- DEMA period used by NN features
NN_DEMA=1

# STOCHRSI
# --- StochRSI smoothing interval
STOCHRSI_INTERVAL=10
# --- StochRSI high threshold
STOCHRSI_HIGH=70
# --- StochRSI low threshold
STOCHRSI_LOW=30
# --- StochRSI persistence/filter length
STOCHRSI_PERSISTENCE=5
# --- RSI period used inside StochRSI
STOCHRSI_RSI=21
# --- Stoch period used inside StochRSI
STOCHRSI_STOCH=21

# CCI
# --- CCI upper threshold
CCI_THRESHOLDS_UP=100
# --- CCI lower threshold
CCI_THRESHOLDS_DOWN=-100
# --- CCI persistence count
CCI_THRESHOLDS_PERSISTENCE=0
# --- CCI constant for calculation
CCI_CONSTANT=0.015
# --- CCI history buffer length
CCI_HISTORY=987
